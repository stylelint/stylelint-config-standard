sudo: false

git:
  depth: 1

branches:
  only:
    - master
    - /^dependabot/.*$/

language: node_js

# cache node modules
cache:
  directories:
    - $HOME/.npm
    - node_modules

node_js:
  - '10'

before_install:
  - npm install -g npm@latest
